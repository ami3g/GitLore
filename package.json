{
  "name": "gitlore-monorepo",
  "private": true,
  "version": "0.2.0",
  "description": "GitLore monorepo — RAG-powered project intelligence for Git history, code, and PR context.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:core": "cd packages/core && npx tsc -b",
    "build:extension": "cd packages/vscode && node esbuild.js",
    "build:webview": "cd packages/vscode && npx vite build --config webview/vite.config.ts",
    "compile": "npm run build:core && npm run build:extension && npm run build:webview",
    "watch:extension": "cd packages/vscode && node esbuild.js --watch",
    "dev:webview": "cd packages/vscode/webview && npx vite build --watch",
    "lint": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
